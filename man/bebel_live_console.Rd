% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{bebel_live_console}
\alias{bebel_live_console}
\title{Live terminal console for BebeLM chats}
\usage{
bebel_live_console(
  x,
  prompt = ">>> ",
  exit_commands = c("/quit", "/exit"),
  on_event = bebel_console_event(),
  check_interrupt = TRUE,
  greedy = FALSE,
  max_gen = NULL,
  max_context = NULL,
  max_think = NULL,
  temperature = NULL,
  top_k = NULL,
  repeat_penalty = NULL
)
}
\arguments{
\item{x}{A \code{BebelModel} or \code{BebelAgent}.}

\item{prompt}{Prompt displayed before reading each user message.}

\item{exit_commands}{Character vector of commands that exit the console.}

\item{on_event}{Event handler used for assistant output.}

\item{check_interrupt}{Check for Ctrl-C during generation.}

\item{greedy}{Use deterministic greedy decoding.}

\item{max_gen, max_context, max_think}{Optional generation limits.}

\item{temperature, top_k, repeat_penalty}{Optional sampling settings.}
}
\value{
Invisibly returns the \code{BebelAgent} used by the console.
}
\description{
Start an interactive terminal chat loop. If \code{x} is a \code{BebelModel}, a new
\code{BebelAgent} is created. If \code{x} is a \code{BebelAgent}, its existing transcript and
caches are reused. Type \verb{/quit} or \verb{/exit} to leave the loop.
}
