% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{bebel_pooled_states}
\alias{bebel_pooled_states}
\title{Extract pooled BebeLM contextual states}
\usage{
bebel_pooled_states(
  model,
  text,
  add_bos = TRUE,
  normalize = TRUE,
  pooling = c("weighted_mean", "mean", "last"),
  token_batch_size = 512L,
  sequence_batch_size = 64L,
  check_interrupt = TRUE
)
}
\arguments{
\item{model}{A \code{BebelModel} object.}

\item{text}{Character vector.}

\item{add_bos}{Whether to prepend the model's beginning-of-sequence token.}

\item{normalize}{L2-normalize each pooled row.}

\item{pooling}{Pooling strategy. \code{"weighted_mean"} weights token positions
\verb{1, ..., n}, \code{"mean"} uses equal weights, and \code{"last"} selects the final
contextual state.}

\item{token_batch_size}{Number of tokens per Rust batched prefill/matmul call.}

\item{sequence_batch_size}{Number of texts per independent-sequence state
batch.}

\item{check_interrupt}{Whether long extraction runs should poll R interrupts
between texts and token batches.}
}
\value{
A \code{bebelPooledStates} numeric matrix with one row per input text and a
\code{state_info} attribute describing the extraction contract.
}
\description{
Runs the causal LFM2.5 model over each input, applies the model's final output
RMSNorm to every token state, and pools those states. \code{"weighted_mean"}
implements the position-weighted pooling baseline introduced for GPT sentence
representations by Muennighoff (2022) \url{https://arxiv.org/abs/2202.08904}.
}
\details{
These are features from a text-generation model, not embeddings from a model
trained for semantic similarity or retrieval. Cosine similarity is therefore
uncalibrated and must be evaluated for the intended task. The same limitation
applies if these vectors are used in a dense index.
}
