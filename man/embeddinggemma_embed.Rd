% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{embeddinggemma_embed}
\alias{embeddinggemma_embed}
\title{Generate EmbeddingGemma text embeddings}
\usage{
embeddinggemma_embed(
  model,
  text,
  task,
  title = NULL,
  dimensions = 768L,
  normalize = TRUE,
  truncate = TRUE,
  check_interrupt = TRUE
)
}
\arguments{
\item{model}{An \code{EmbeddingGemmaModel} object.}

\item{text}{Character vector to embed.}

\item{task}{One of \code{"retrieval_query"}, \code{"retrieval_document"},
\code{"question_answering"}, \code{"fact_verification"}, \code{"classification"},
\code{"clustering"}, \code{"semantic_similarity"}, \code{"code_retrieval"},
\code{"summarization"}, or \code{"raw"}. \code{"raw"} adds no task prompt and should
only be used with already formatted model input.}

\item{title}{Optional document-title scalar or vector. It is valid only for
\code{task = "retrieval_document"}; \code{NULL} uses the model's \code{"none"} title.}

\item{dimensions}{Output dimension: 768, 512, 256, or 128.}

\item{normalize}{L2-normalize each output row. Keep this \code{TRUE} for cosine
similarity and Matryoshka embeddings.}

\item{truncate}{Truncate overlong inputs to the model's 2048-token context,
preserving BOS and EOS. If \code{FALSE}, overlong inputs fail.}

\item{check_interrupt}{Poll for R user interrupts during tokenization and
between bounded packed inference batches.}
}
\value{
An \code{embeddingGemmaEmbeddings} numeric matrix with one row per input.
Its \code{embedding_info} attribute records prompts, token counts, truncation,
dimensions, and normalization.
}
\description{
Runs the retrieval-trained bidirectional Gemma encoder, attention-mask mean
pooling, and both learned dense projection layers. The requested task is
deliberately mandatory: EmbeddingGemma was trained with different prompt
contracts for queries, documents, semantic similarity, and other tasks.
}
\details{
For retrieval, prefer \code{embeddinggemma_embed_query()} and
\code{embeddinggemma_embed_document()} so query and document prompts cannot be
confused. Matryoshka dimensions 512, 256, and 128 select the leading
dimensions of the 768-vector and then re-normalize, as specified by the
model card.
}
