% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agent-loop.R
\name{bebel_agent_loop}
\alias{bebel_agent_loop}
\title{Create a stateful BebeLM agent loop}
\usage{
bebel_agent_loop(
  agent,
  tools = list(),
  context = new.env(parent = emptyenv()),
  policy = bebel_loop_policy(),
  hooks = list(),
  extensions = list(),
  session = TRUE,
  parse_tool_call = bebel_parse_tool_calls,
  on_event = NULL,
  check_interrupt = TRUE
)
}
\arguments{
\item{agent}{An object implementing \link{AgentBackend}.}

\item{tools}{A list of \code{bebel_tool()} objects or named functions.}

\item{context}{Private mutable context passed to tools and hooks.}

\item{policy}{A \code{\link[=bebel_loop_policy]{bebel_loop_policy()}} object.}

\item{hooks}{Optional named hooks. Loop hooks may observe \code{state_change},
\code{queue_update}, \code{message_start}, \code{message_end}, \code{model_event},
\code{tool_request}, \code{tool_result}, \code{tool_error}, \code{tool_denied}, \code{observation},
\code{command_start}, \code{command_end}, and \code{loop_end}.}

\item{extensions}{Optional list of \code{\link[=bebel_extension]{bebel_extension()}} objects.}

\item{session}{Session persistence setting. \code{TRUE} creates an \code{agentSession}
under \code{agent_session_dir()}, \code{FALSE}/\code{NULL} disables persistence, an
\code{agentSession} reuses that store, and a character path opens a JSONL session.}

\item{parse_tool_call}{Function converting tool-call text into one or more
call records.}

\item{on_event}{Optional event callback or handler list for model stream events.}

\item{check_interrupt}{Check for Ctrl-C during generation.}
}
\value{
A \code{bebelAgentLoop} environment.
}
\description{
\code{bebel_agent_loop()} is the UI-independent controller inspired by Pi's
Agent/AgentSession versus InteractiveMode split. It owns lifecycle state,
queues, policy, hooks, and tool dispatch. Consoles, RPC handlers, and TUIs
should consume this loop rather than embedding agent business logic.
}
