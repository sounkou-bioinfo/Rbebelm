% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agent-layer.R
\name{bebel_r_agent_start}
\alias{bebel_r_agent_start}
\title{Launch an R-native Rbebelm console from weights}
\usage{
bebel_r_agent_start(
  weights = Sys.getenv("BEBELM_WEIGHTS_FILE", "LFM2.5-8B-A1B-Q4_K_M.gguf"),
  num_threads = as.numeric(Sys.getenv("BEBELM_NUM_THREADS", "2")),
  env = .GlobalEnv,
  cwd = getwd(),
  allow_eval = TRUE,
  greedy = TRUE,
  max_gen = as.numeric(Sys.getenv("BEBELM_AGENT_MAX_GEN", "256")),
  max_context = 4096,
  max_think = as.numeric(Sys.getenv("BEBELM_AGENT_MAX_THINK", "48")),
  temperature = 0.8,
  top_k = 50,
  repeat_penalty = 1.1,
  prompt = "bebel> ",
  max_steps = 4L,
  show_stats = TRUE,
  blank_limit = 10L,
  prompt_style = c("compact", "full")
)
}
\arguments{
\item{weights}{GGUF weights file. Defaults to \code{BEBELM_WEIGHTS_FILE}, then
\code{"LFM2.5-8B-A1B-Q4_K_M.gguf"} in the working directory.}

\item{num_threads}{Optional Rayon thread count passed to \code{\link[=bebel_model_load]{bebel_model_load()}}.}

\item{env}{Environment shared by \verb{/r}, \verb{/rplot}, \code{r_objects}, and optional code-evaluation tools.}

\item{cwd}{Working directory for file tools and \verb{/rplot} output.}

\item{allow_eval}{Whether to include \code{r_eval} and \code{r_plot} tools that the model can call.}

\item{greedy, max_gen, max_context, max_think, temperature, top_k, repeat_penalty}{Generation options passed to \code{\link[=bebel_r_agent]{bebel_r_agent()}}.}

\item{prompt}{Prompt string for \code{\link[=bebel_r_agent_console]{bebel_r_agent_console()}}.}

\item{max_steps}{Maximum assistant/tool iterations per user prompt.}

\item{show_stats}{Whether to print token/timing stats after each turn.}

\item{blank_limit}{Number of consecutive blank inputs before exiting the console. Set to \code{Inf} to never auto-exit on blanks.}

\item{prompt_style}{Tool prompt verbosity passed to \code{\link[=bebel_r_agent]{bebel_r_agent()}}.}
}
\value{
Invisibly returns the \code{bebelRAgent} session after the console exits.
}
\description{
Convenience wrapper for loading a model, creating a \code{\link[=bebel_r_agent]{bebel_r_agent()}}, and
entering \code{\link[=bebel_r_agent_console]{bebel_r_agent_console()}}. This keeps the loaded model object local
to the launcher while the agent tools, \verb{/r}, and \verb{/rplot} commands share \code{env}.
}
