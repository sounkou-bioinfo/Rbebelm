% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{bebel_assistant_turn_tool_stop}
\alias{bebel_assistant_turn_tool_stop}
\title{Open an assistant turn and stop when a tool call closes}
\usage{
bebel_assistant_turn_tool_stop(agent, on_event = NULL, check_interrupt = TRUE)
}
\arguments{
\item{agent}{A \code{BebelAgent} object.}

\item{on_event}{Event callback, named list of event-specific handlers, or
\code{NULL}. Event types are \code{bebel_event_types()}. Delta events contain \code{delta},
\code{id}, and \code{index}; final events contain accumulated \code{content} or \code{text}.}

\item{check_interrupt}{Check for Ctrl-C during prefill and before every decoded token.}
}
\value{
A \code{bebelAssistantTurnResult} list.
}
\description{
This low-level variant mirrors upstream BebeLM's tool driver stop semantics:
generation stops with \code{stop == "tool_call"} after \verb{<|tool_call_end|>} so the
caller can execute the requested tool and append one tool-result turn.
}
