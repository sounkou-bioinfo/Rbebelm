% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/file-search.R
\name{bebel_file_finder}
\alias{bebel_file_finder}
\alias{bebel_file_search}
\title{Create a native fuzzy file finder}
\usage{
bebel_file_finder(
  root = getwd(),
  frecency_db_path = "",
  history_db_path = "",
  enable_mmap_cache = FALSE,
  enable_content_indexing = FALSE,
  watch = FALSE,
  ai_mode = TRUE,
  wait_timeout_ms = 10000
)

bebel_file_search(
  finder = getwd(),
  query = "",
  current_file = "",
  max_threads = 0,
  offset = 0,
  limit = 50,
  combo_boost_score_multiplier = 100,
  min_combo_count = 3,
  wait_timeout_ms = 10000
)
}
\arguments{
\item{root}{Project directory to index.}

\item{frecency_db_path}{Optional FFF frecency database path. Empty string
disables frecency persistence.}

\item{history_db_path}{Optional FFF query-history database path. Empty string
disables query-history persistence.}

\item{enable_mmap_cache}{Enable FFF mmap cache warmup.}

\item{enable_content_indexing}{Enable FFF content indexing.}

\item{watch}{Start FFF's filesystem watcher for live updates.}

\item{ai_mode}{Use FFF's AI-agent mode.}

\item{wait_timeout_ms}{Milliseconds to wait for initial indexing or query
readiness.}

\item{finder}{A \code{BebelFileFinder} object, or a root path from which a
temporary finder should be created for one search.}

\item{query}{Fuzzy query string.}

\item{current_file}{Optional currently focused file for FFF scoring.}

\item{max_threads}{Maximum FFF search threads; \code{0} means auto.}

\item{offset}{Result offset for pagination.}

\item{limit}{Maximum number of rows to return.}

\item{combo_boost_score_multiplier}{FFF combo boost multiplier.}

\item{min_combo_count}{Minimum combo count before boost.}
}
\value{
A \code{BebelFileFinder} object.
}
\description{
\code{bebel_file_finder()} creates a persistent native FFF/\code{fff-c}-based file
index for a project directory. \code{bebel_file_search()} queries that index and
returns a data frame suitable for consoles, RPC clients, and the standalone
\verb{tui/} file picker.
}
\details{
The FFF backend is native-only. In webR/wasm this API is present but creating
a finder raises an explicit unsupported error so the rest of the package can
still load.
}
