% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{embeddinggemma_tokenize}
\alias{embeddinggemma_tokenize}
\title{Tokenize EmbeddingGemma model input}
\usage{
embeddinggemma_tokenize(model, text, task, title = NULL, truncate = TRUE)
}
\arguments{
\item{model}{An \code{EmbeddingGemmaModel} object.}

\item{text}{Character vector to embed.}

\item{task}{One of \code{"retrieval_query"}, \code{"retrieval_document"},
\code{"question_answering"}, \code{"fact_verification"}, \code{"classification"},
\code{"clustering"}, \code{"semantic_similarity"}, \code{"code_retrieval"},
\code{"summarization"}, or \code{"raw"}. \code{"raw"} adds no task prompt and should
only be used with already formatted model input.}

\item{title}{Optional document-title scalar or vector. It is valid only for
\code{task = "retrieval_document"}; \code{NULL} uses the model's \code{"none"} title.}

\item{truncate}{Truncate overlong inputs to the model's 2048-token context,
preserving BOS and EOS. If \code{FALSE}, overlong inputs fail.}
}
\value{
A named list containing integer \code{ids}, legible token pieces, the
task-formatted text, and a truncation flag.
}
\description{
Applies the same mandatory task formatting as \code{embeddinggemma_embed()} and
returns the exact BOS/content/EOS token sequence consumed by the encoder.
}
