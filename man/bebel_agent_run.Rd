% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{bebel_agent_run}
\alias{bebel_agent_run}
\title{Run a BebeLM agent with R tool dispatch}
\usage{
bebel_agent_run(
  agent,
  tools = list(),
  context = new.env(parent = emptyenv()),
  hooks = list(),
  parse_tool_call = bebel_parse_tool_calls,
  max_steps = 4,
  on_event = NULL,
  check_interrupt = TRUE
)
}
\arguments{
\item{agent}{A \code{BebelAgent} object.}

\item{tools}{A list of \code{bebel_tool()} objects or named functions.}

\item{context}{Private run context passed to tools and hooks but not appended to the model transcript.}

\item{hooks}{Optional named list of hooks: \code{turn_start}, \code{event}, \code{tool_request},
\code{tool_result}, \code{tool_error}, \code{turn_end}.}

\item{parse_tool_call}{Function converting tool-call content to either one \code{list(name, arguments, raw)} or a list of such calls.}

\item{max_steps}{Maximum assistant/tool iterations.}

\item{on_event}{Optional event callback or handler list for model events.}

\item{check_interrupt}{Check for Ctrl-C during generation.}
}
\value{
A \code{bebelAgentRun} list with turns, tool calls, and final agent info.
}
\description{
This is an Agent-first orchestration loop. It observes \code{tool_call_end} events,
parses tool calls, invokes matching R tools with private \code{context}, appends
tool results to the agent transcript, and continues generation.
}
