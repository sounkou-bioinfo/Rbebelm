% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agent-loop.R
\name{bebel_loop_rpc_server}
\alias{bebel_loop_rpc_server}
\title{Serve a generic Rbebelm agent loop over HTTP(S)}
\usage{
bebel_loop_rpc_server(loop, url = "http://127.0.0.1:8080", tls = NULL)
}
\arguments{
\item{loop}{A \code{bebelAgentLoop}.}

\item{url}{URL to listen on, e.g. \code{"http://127.0.0.1:8080"} or
\code{"https://0.0.0.0:8443"}.}

\item{tls}{Optional TLS configuration from \code{nanonext::tls_config()} for
HTTPS/WSS endpoints.}
}
\value{
A \code{nanoServer} object from \code{nanonext}.
}
\description{
This optional SDK surface exposes a backend-agnostic \code{\link[=bebel_agent_loop]{bebel_agent_loop()}} over
a transport endpoint with \code{GET /stream} NDJSON events, \code{POST /command} typed
commands, and \code{POST /rpc} JSON-RPC compatibility. The endpoint may be local
HTTP, remote HTTP, or HTTPS/TLS when \code{nanonext} is configured with TLS.
External frontends such as the native \code{rbebelm-tui} binary call the loop
protocol and never assume the backend is a concrete \code{BebelAgent}.
}
