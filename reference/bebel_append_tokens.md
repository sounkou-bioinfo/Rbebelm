# Append token ids to a BebeLM agent transcript

Append token ids to a BebeLM agent transcript

## Usage

``` r
bebel_append_tokens(agent, ids)
```

## Arguments

- agent:

  A `BebelAgent` object.

- ids:

  Integer token ids.

## Value

Invisibly returns `agent`.
