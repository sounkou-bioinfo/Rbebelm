# Persistent native FFF fuzzy file finder.

Persistent native FFF fuzzy file finder.

## Usage

``` r
BebelFileFinder
```

## Format

An object of class `Rbebelm::BebelFileFinder__bundle` (inherits from
`savvy_Rbebelm__sealed`) of length 1.
