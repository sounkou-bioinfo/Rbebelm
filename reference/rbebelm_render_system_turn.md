# Render an upstream BebeLM ChatML system turn, optionally with tool schemas.

Render an upstream BebeLM ChatML system turn, optionally with tool
schemas.

## Usage

``` r
rbebelm_render_system_turn(message, tool_names, tool_schemas)
```
