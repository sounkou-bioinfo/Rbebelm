# Append raw text to a BebeLM agent transcript

Append raw text to a BebeLM agent transcript

## Usage

``` r
bebel_append(agent, text)
```

## Arguments

- agent:

  A `BebelAgent` object.

- text:

  Raw text to append.

## Value

Invisibly returns `agent`.
