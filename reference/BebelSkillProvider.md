# BebeLM skill provider interface

Skill providers list and load reusable instructions or workflow
snippets.

## Usage

``` r
BebelSkillProvider
```

## Format

An object of class `s7contract::s7_interface` (inherits from
`S7_object`) of length 1.
