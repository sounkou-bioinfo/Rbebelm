# Return an agent session tree

Return an agent session tree

## Usage

``` r
bebel_session_tree(session)
```

## Arguments

- session:

  An `bebelSession`.

## Value

A nested list of tree nodes with `entry`, `children`, and optional
`label`.
