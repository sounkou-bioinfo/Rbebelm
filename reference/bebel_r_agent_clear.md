# Clear an Rbebelm R agent session

Clear an Rbebelm R agent session

## Usage

``` r
bebel_r_agent_clear(session)
```

## Arguments

- session:

  A `bebelRAgent`.

## Value

Invisibly returns `session`.
