# Loaded EmbeddingGemma GGUF model.

Loaded EmbeddingGemma GGUF model.

## Usage

``` r
EmbeddingGemmaModel
```

## Format

An object of class `Rbebelm::EmbeddingGemmaModel__bundle` (inherits from
`savvy_Rbebelm__sealed`) of length 1.
